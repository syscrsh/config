* Just a few config files, nothing to see here.

* I might add something later that allows for easy deployment, like having a shellscript to automatically set these up.

* Also needs a bit of cleanup.

